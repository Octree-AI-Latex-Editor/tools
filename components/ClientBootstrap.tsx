'use client';

import '@/lib/promise-polyfill';

/**
 * Client-side bootstrap component.
 * Runs once on app load for any client-side initialization.
 * The generator can extend this with analytics, auth, etc.
 */
export default function ClientBootstrap() {
  return null;
}
